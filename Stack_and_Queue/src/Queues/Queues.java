package Queues;

import java.util.LinkedList;
import java.util.Queue;

public class Queues {
    public static void main(String[] args) {
        //here the object class is linkedList type because the queue is interface not a  class u have to initalize the methord of interface to use them
        Queue<Integer> queue=new LinkedList<>();


    }
}
