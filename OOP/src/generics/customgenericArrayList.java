package generics;
// we can't make the variable of the class static because static variable shared by all the object of the class
//if we use this object get confused because every object is of different type in the generic class and static variable  does not depend on the object of the class

public class customgenericArrayList<T> {

}
