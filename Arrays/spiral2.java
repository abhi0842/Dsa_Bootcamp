public class spiral2 {
    public static void main(String[] args) {
        int c1-0,c2=n-1;
        int[][]arr=new int[n][n];
        int val-1;
        while(r1<=r2 && c1<=c2){
            // moving left to right
            for(intc-c1;i<=c2;c++)arr[rl][c]-val ++;
            // move down
            for(intr=r1+1;i<=r2;r++)arr[r][<2]=val ++;
            // move right to left
            // move up
            if(r1<r2 && cl<c2){
            }
                 // move right to left
                 for(intc-c2-1;c>cl1;c--)arr[r2][c]-val ++;
                 // move up
                 for(intr=r2;r>r1;r--)arr[r][c1]=val ++;
            ri ++;
            125
            C1+
    }
}
